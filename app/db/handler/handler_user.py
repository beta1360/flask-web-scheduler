from data.user import User

def add_user(req):
    id = req["id"]
    password = req["password"]
    name = req["name"]
    rank = req["rank"]

    #user = User(id, password, name, rank)

    '''
    DB Connection
    '''


def modify_user(req):
    id = req["id"]
    password = req["password"]
    name = req["name"]
    rank = req["rank"]

    #user = User(id, password, name, rank)

    '''
    DB Connection
    '''


def delete_user(no):
    '''
    DB Connection
    '''


